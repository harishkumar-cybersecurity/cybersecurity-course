***Network basics ***
**IP ADD**
IP address are the unique address that are given by the network to the user to communicate
ipv4  
- it is a 4 decimal octet
-it range is 0 to 255
-it is consist of 32 bit
this ip add iss seprate by 4 part first three aree known as network id and the last one is host id
basic ip add is **192.168.0.1**
 ipv6
-it is a 8 segment unlike ip v4
-it consist of 128 bit 
public ip
-the ip address that are given to the user by the server
private IP
it is a unique address that are provided to the user

**TOPOLOGY**
1.star topology
2.bus topology
3.ring topology
4.mesh topology

**Switch and Router**
*switch are used to control the communication between the device it serve the data according to the ip add and mac add
*router connect multiple network device 

**Sub net**
it use to divid a large network into smaller network
ex:255.255.255.0

**OSI layer model**
1.physical layer
2.Datalink layer
3.network layer
4.transport layer
5.Session layer
6.presentation layer
7.application layer

**TCP/IP model**
 it is a updated model of osi model and the 7 layer are reduced into 4 layers
 it is the current using model 
 1.application layer
 2.transport layer
 3.internet layer
 4.networking layer

 **Tcp three way handshaking**
there are basic 4 steps to start a communication is 

client--syn--server
client--syn+ack--server
client--ack--server

**UDP(user datagram protocol)**
#it is more quicker than the tcp 
#it is not reliable like tcp data packet can be lost 
#used in voice call,streeming etc..












 
